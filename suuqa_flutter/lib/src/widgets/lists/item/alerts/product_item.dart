import 'package:flutter/material.dart';
import 'package:suuqa_common/suuqa_common.dart';

class ProductItem extends StatelessWidget {
  final Alert alert;

  ProductItem({this.alert});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
